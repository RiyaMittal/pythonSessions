import calculation_utils

# using as an alias:
# import calculation_utils as utils

# importing the functionality
# from calculation_utils import add, sub

# to import all u can use * , so we can use the functions directly without prefixing the module name
# from calculation_utils import *

print(calculation_utils.add(1, 2))
print(calculation_utils.sub(3, 4))
print(calculation_utils.mul(3, 4))
print(calculation_utils.div(8, 4))
